# Railway Environment Variables Configuration
# Set these variables in your Railway project settings

# REQUIRED: Django Configuration
DJANGO_SECRET_KEY=your-secret-key-here-generate-a-new-one
DJANGO_DEBUG=False
DJANGO_ALLOWED_HOSTS=web-production-a944f.up.railway.app

# AUTOMATIC: Railway Environment Variables (set by Railway)
# RAILWAY_ENVIRONMENT=production (automatically set)
# DATABASE_URL=postgresql://... (automatically provided by Railway)
# PORT=8000 (automatically set)

# OPTIONAL: Additional Configuration
BACKGROUND_REMOVAL_METHOD=auto

# DEPLOYMENT CHECKLIST:
# 1. Set DJANGO_SECRET_KEY to a secure random string (50+ characters)
# 2. Ensure DJANGO_DEBUG=False for production
# 3. Update DJANGO_ALLOWED_HOSTS with your Railway domain
# 4. Add PostgreSQL database service in Railway dashboard
# 5. Deploy from your GitHub repository

# Example secret key generation (run in Python):
# from django.core.management.utils import get_random_secret_key
# print(get_random_secret_key())
